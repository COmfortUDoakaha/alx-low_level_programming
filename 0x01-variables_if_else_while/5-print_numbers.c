#include <stdio.h>
/**
 * main - Prints all single digit of base
 * 10 from 0
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int i;

	for (i = 0; i <= 9; i++)
		printf("%d", i);
	putchat('\n');
	return (0);
}
